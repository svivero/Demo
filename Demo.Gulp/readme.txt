Install gulp
    npm install gulp -g

Create gulp project
    npm init

Add gulp as a dev dependency project in package.json
    npm install gulp --save-dev

Create a gulp configuration file
	create gulpfile.js in root